const config = {
    width: 800,
    height: 600,
    parent: "container",
    type: Phaser.CANVAS,
    backgroundColor: '#392542',
    scene: [SceneA, SceneB]
    // scene: [SceneA]
}

var game = new Phaser.Game(config);

function preload(){
    
}

function create(){
   
}

function update(time, delta){
    
}